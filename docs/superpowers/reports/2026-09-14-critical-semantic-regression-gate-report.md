# NovaQ Critical Semantic Regression Gate — Verification Report\n\n**Date:** 2026-09-14\n**Specification:** `docs/superpowers/specs/2026-09-13-critical-semantic-regression-gate.md`\n**Plan:** `docs/superpowers/plans/2026-09-13-critical-semantic-regression-gate-plan.md`\n**Result:** Passed; page-by-page visual refinement is unblocked within the approved safety boundary\n\n## Verified outcomes\n\n| Protected semantic rule | Implemented guard | Failure mode prevented |\n|---|---|---|\n| Unknown setup values remain unknown | Canonical Setup and Guided Setup tests assert exact `unknown`/`null` payloads; backend test follows create, persistence, unrelated patch, and fetch | Omitted answers cannot silently become shared queue, unlimited capacity, not-modeled abandonment, zero, or another analytical value |\n| Preset copy matches optimizer behavior | Every preset test binds its visible constraint copy to the exact utilization/wait payload | Low-wait and balanced profiles cannot claim independent objectives while the optimizer still minimizes configured cost |\n| Validation uses persisted run parameters | Simulation renders verdicts from saved validation evidence and warns when next-run controls differ; backend Decision test reads the saved cap | Editing a current control cannot retroactively change an earlier run's pass/fail meaning |\n| Report preview matches export | Frontend previews exact PDF sections and Excel sheets; generator tests inspect PDF headings and both Excel sheet branches | Preview cannot advertise Simulation Validation, ROI, Methodology, Appendix, or other nonexistent sections |\n| Navigation targets resolve | Router inventory, Dashboard link, and all workflow Previous/Next targets are asserted | Stale top-level `/optimize`, `/simulate`, and `/compare` links cannot recur silently |\n| Missing analytical values never become zero | Shared complete-value helpers require every input to be finite; formatters distinguish genuine zero from unavailable values; tables/charts use those contracts | `null`, missing, NaN, infinity, or incomplete totals cannot become a valid zero through frontend aggregation or display |\n| Decision evidence is scenario-scoped and current | Integration coverage creates evidence for one scenario, switches selection, and proves the previous DES/validation/Decision are absent; existing upstream-rerun staleness coverage remains green | Obsolete evidence cannot appear current for a different scenario or after a newer relevant run |\n\nThe repository-backed executable order remains `Setup → Current → Optimize → Compare → Simulate → Decision → Reports`. The requested Simulate-before-Compare ordering remains intentionally deferred because the current API requires a verified scenario selection from Compare before simulation.\n\n## Implementation scope\n\n- Corrected Guided Setup's unanswered-value mapping without changing the backend schema or explicit choices.\n- Reworded optimization presets as constraint profiles; all numeric settings and optimizer calls are unchanged.\n- Bound Simulation validation presentation to persisted evidence parameters.\n- Replaced the generic report preview with exact format-specific export structure.\n- Routed Dashboard workflow shortcuts through the canonical analysis selector and added route resolution coverage.\n- Consolidated complete finite-value aggregation/formatting behavior for comparison, optimization, and cost charts.\n- Added behavior-level frontend and backend semantic regression tests.\n\nNo backend production code, queueing formula, model selection, optimizer implementation, DES/Monte Carlo execution, Decision derivation, authentication/CSRF behavior, provenance/staleness logic, API contract, report calculation, or report unit conversion was changed.\n\n## Verification evidence\n\n- Focused backend semantics: **56 passed**.\n- Focused frontend semantics: **51 passed**.\n- Full backend: **466 passed, 3 skipped, 6 subtests passed**.\n- Full frontend: **31 files, 140 tests passed**.\n- `python -m ruff check .`: passed.\n- `python -m mypy .`: passed, 97 source files checked.\n- `npm run typecheck`: passed.\n- `npm run lint`: passed with two pre-existing Fast Refresh warnings in `NovaQInsights.tsx`.\n- `npm run build`: passed; the existing large Plotly chunk warning remains non-blocking.\n- Locale parity: passed, **565 keys** in both English and Tagalog.\n- `git diff --check`: passed; Windows LF/CRLF notices only.\n\n## Gate decision\n\nThe mandatory semantic gate is green. Major UI refinement may proceed only through the approved page-by-page plan and shared design system. These tests are now required pre-change and post-change regression gates; visual completion alone is insufficient.\n