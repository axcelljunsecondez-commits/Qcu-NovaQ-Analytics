# NovaQ Critical Semantic Regression Gate\n\n**Status:** Approved pre-redesign gate\n**Date:** 2026-09-13\n**Scope:** Behavior-level tests and the smallest already-approved P0 truth repairs required to make them pass\n\n## 1. Verified problem\n\nPage-by-page redesign could preserve rendered shapes while changing or concealing protected product meaning. The current suite has strong engine and API coverage, but it does not yet bind every affected UI statement to the authoritative setup, optimizer, saved-run, and report-export contracts.\n\n## 2. Repository evidence and intended behavior\n\n| Semantic contract | Authoritative evidence | Current UI risk | Required regression behavior |\n|---|---|---|---|\n| Unknown setup remains unknown | `backend/api/analysis_schemas.py` defines `unknown` enum values and nullable dependent fields; `AnalysisCreate` uses `unknown_queue_setup()` | Guided Setup defaults an unanswered queue to shared and maps nullable capacity/abandonment answers to definite modes | Untouched/unsure setup values serialize as `unknown`/`null`; the schema's explicit `staffing_varies_by_period = false` remains the only relevant documented boolean default |\n| Presets are constraints, not objectives | `optimization.py` minimizes configured total cost among candidates satisfying `target_utilization` and optional `max_wait_minutes`; current presets pass 0.85/null, 0.65/2, and 0.75/5 | Labels claim distinct minimum-cost/minimum-wait/balanced objectives | Each preset names a constraint profile, states the exact utilization/wait settings, states that cost minimization remains the objective, and sends the unchanged values |\n| Validation uses its saved run | `WorkflowJob.params` persists run configuration; `_derive_decision()` reads `validation.params.mc_failure_rate_cap` | Simulation recomputes the displayed verdict from the editable next-run cap | The displayed verdict and row state use the saved cap; changing current controls shows a rerun notice but cannot rewrite old evidence |\n| Preview matches export | PDF generator emits title, Executive Summary, Segment Comparison, and Recommendations; Excel emits Summary, Segments, and a Recommendations sheet when recommendation evidence is supplied | Reports advertises ten generic sections absent from both artifacts | Preview is format-specific and contains only generator-backed labels; backend tests inspect real generator output/branches and frontend tests reject unsupported sections |\n| Navigation targets resolve | `frontend/src/router.tsx` defines analysis-scoped workflow routes; `WorkflowNavigator` derives dynamic Previous/Next links | Dashboard quick actions point to nonexistent top-level `/optimize`, `/simulate`, and `/compare` routes; route drift could recur during redesign | Global links resolve to registered routes, analysis-dependent actions enter through the analysis selector, and every workflow Previous/Next target is canonical |\n| Missing analytical values never become zero | Backend result/report integrity tests preserve unavailable fields as `null`/N/A; frontend aggregations use several `?? 0` fallbacks | Missing cost, wait, utilization, or staffing inputs can become apparently valid zero-valued summaries or chart components | Formatters reject non-finite values, and averages/totals render only when every required source value is finite; genuine numerical zero remains valid |\n| Decision evidence remains scenario-scoped and current | `workflow._job_matches()` filters by analysis/scenario; `_current_evidence()` filters DES/MC/validation/Decision and invalidates a Decision when referenced evidence IDs change | A visual refactor could present prior-scenario or superseded evidence as current | Tests switch scenarios and rerun upstream evidence to prove obsolete DES/validation/Decision records are absent or stale while the persisted selection remains owner-scoped |\n\n## 3. Affected files\n\n- Frontend setup, Optimize, Simulation, Reports, Dashboard, comparison/chart/format helpers, router, and workflow-navigation tests, plus the minimum related page/copy code.\n- Both locale files for corrected user-visible wording.\n- New backend semantic-contract tests and strengthened report-generator tests.\n- The approved refinement plan, `memory.md`, and `handoff.md` for durable gate status.\n\n## 4. Protected behavior\n\nDo not change queueing mathematics, centralized model selection, optimizer objective/constraints/tie-breaking, request or response contracts, DES/Monte Carlo execution, workflow evidence persistence, Decision derivation, authentication/CSRF, report generation/calculation, or report wait-time units. Existing scenario selection, staleness, provenance, and export authorization remain unchanged.\n\n## 5. Allowed changes\n\n- Add behavior-level tests using real payloads, persisted parameters, and generated artifacts.\n- Correct misleading preset copy without changing preset values or optimizer calls.\n- Make Guided Setup preserve backend-supported unknown values pending its approved consolidation.\n- Read validation display thresholds from persisted evidence and label edited controls as next-run settings.\n- Replace the generic report-preview list with the exact current PDF/Excel structure.\n- Point invalid Dashboard workflow shortcuts at the existing analysis selector and test registered dynamic routes without changing route architecture or workflow prerequisites.\n- Consolidate complete finite-value aggregation/formatting so unavailable inputs remain unavailable in comparison, optimization, and cost-chart presentation.\n- Add scenario-switch and evidence-ID staleness regression coverage around the existing workflow implementation.\n\n## 6. Prohibited changes\n\n- Snapshot-only assertions as substitutes for payload/artifact behavior.\n- New analytical defaults, frontend validation thresholds, optimization objectives, report sections, projections, ROI, or evidence states.\n- Reimplementation of backend Decision or optimizer logic in React.\n- Report-generator or API changes merely to make the preview easier to implement.\n- Page-wide styling, navigation, or component redesign during this gate.\n- Reordering Compare and Simulate, inventing redirect destinations, or changing scenario-selection prerequisites.\n\n## 7. Acceptance criteria\n\n1. Tests prove unknown/null setup values survive UI submission and API persistence without becoming analytical values.\n2. Tests bind all three preset descriptions to their unchanged submitted constraints and reject distinct-objective claims.\n3. A saved-cap-versus-edited-cap test proves the rendered validation verdict remains tied to saved evidence and exposes a rerun notice.\n4. Backend tests inspect PDF headings and exact Excel sheet branches; frontend tests show only those format-specific labels.\n5. Router inventory, Dashboard links, and every workflow Previous/Next target resolve to a canonical route; invalid top-level workflow paths are rejected.\n6. Tests distinguish genuine zero from missing/non-finite analytical values in formatters, aggregates, comparison totals, and cost charts.\n7. Scenario-switch and upstream-rerun tests prove prior or superseded Decision evidence cannot appear current.\n8. Relevant protected optimizer, workflow, report, analysis, and frontend suites pass.\n9. No protected production file or contract changes.\n\n## 8. Regression risks and verification\n\n- **Risk:** Treating a missing persisted cap as a failed validation. **Control:** render verdict unavailable rather than substitute the current field or a constant.\n- **Risk:** Correct copy drifts from actual preset payloads. **Control:** one interaction test asserts visible copy and the API call together.\n- **Risk:** Preview drifts as exporters evolve. **Control:** generator-output tests and frontend exact-label tests must be updated in the same change.\n- **Risk:** Existing dirty workflow/simulation work is overwritten. **Control:** add new backend test files where practical and inspect the final diff.\n\nRun the focused frontend page tests, `tests/test_critical_semantics.py`, `tests/test_report_export.py`, protected analysis/optimization/workflow/report suites, and then the repository gates required by `AGENTS.md` before declaring this gate complete.\n