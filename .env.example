# NovaQ local/deployment environment template.
# Copy this file to .env and change every placeholder before any real deployment.

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-this-admin-password

# Keep 0 for local http://localhost. Use 1 only behind HTTPS/TLS.
SECURE_COOKIES=0
