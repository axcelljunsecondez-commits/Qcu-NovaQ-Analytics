# Interface review correction

Browser review reproduced unavailable recommendations rendered as literal null and zero-cashier staffing summaries. Suppress staffing summaries when results are stale or incomplete, render null staffing as an em dash, and translate infeasibility wording covering every enabled constraint. Preserve API payloads and snapshot verification. Use disposable fixtures only.

Validation review also reproduced unlabeled hour-valued simulated waits beside minute-valued MC waits, and a residual-risk verdict incorrectly attributing DES failures to the MC allowance. Display simulated waits in explicitly labeled minutes and separate the two criteria in bilingual wording. The user subsequently authorized rebuilding and starting the main local Docker Compose stack after verification.

Repeated login regression failures survived the longer asynchronous wait. Navigation must wait until the successful login is reflected in the rendered authenticated context, so the protected-route guard cannot redirect a newly authenticated user back to login before query observers are notified.
