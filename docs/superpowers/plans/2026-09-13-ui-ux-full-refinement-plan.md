# NovaQ Full UI/UX Refinement Implementation Plan\n\n**Status:** Approved execution plan\n**Date:** 2026-09-13\n**Specification:** `docs/superpowers/specs/2026-09-13-ui-ux-full-refinement.md`\n**Implementation status:** Not started by this plan\n\n## Execution rules\n\n- Execute in order. Steps 1-9 are Phase 1 trust repair. Do not begin systemwide refinement until Step 10 passes.\n- Read `design-system/novaq/MASTER.md` before every UI step. The shared-system definition is complete and mandatory; a conflicting page override requires repository evidence and separate approval.\n- Complete `docs/superpowers/plans/2026-09-13-critical-semantic-regression-gate-plan.md` before page-by-page redesign. Its behavior-level tests are mandatory regression coverage for later steps.\n- Steps 2-9 make the smallest truth-repair UI changes and must use Master semantics for anything they touch. Broad page-by-page visual refinement begins only after Steps 11-13 implement the shared foundations.\n- Use test-first changes: add or correct a focused failing test, make the smallest implementation change, then rerun that focused test.\n- Keep existing user changes intact. Record the starting `git status --short` and do not edit unrelated dirty files.\n- Do not modify protected backend/API/report-generator files unless a newly proven defect is documented and separately approved.\n- Add every user-facing string to both locale files in the same step.\n- If current source contradicts this plan, stop that step and amend the specification with evidence before proceeding.\n\n## Step 1 — Capture protected baselines and test harnesses\n\n- **What:** Record the current diff and run focused baselines for model selection, optimization, scenarios, simulation equivalence, workflow evidence/Decision, reports, auth/CSRF, and locale keys. Add a reusable recursive locale-key symmetry test if none exists.\n- **Where:** Existing tests under `tests/`; `frontend/src/lib/http.test.ts`; a new `frontend/src/lib/i18n.test.ts` for locale symmetry; no production behavior.\n- **Why:** The refinement must prove non-regression, and the worktree already contains unrelated modifications that must not be confused with refinement changes.\n- **Preserve:** All analytical outputs, API schemas, evidence rows, Decision responses, auth cookies/headers, and report artifacts.\n- **Verify:** Run `git status --short`; `python -m pytest tests/test_model_selection.py tests/test_optimization.py tests/test_scenario_integrity.py tests/test_simulation.py tests/test_workflow_api.py tests/test_reports_api.py tests/test_auth_api.py tests/test_saas_auth.py tests/test_production_hardening.py -x --tb=short`; and, from `frontend/`, `npm test -- src/lib/http.test.ts src/lib/i18n.test.ts`. Save exact baseline counts in the eventual verification report.\n\n## Step 2 — Implement required one-time onboarding and Help replay\n\n- **What:** Put onboarding behind authentication but outside the completion guard. Add a `RequireOnboarding` route guard that consumes the existing onboarding status endpoint, preserves the requested destination, handles loading/error without redirect loops, and bypasses the completion redirect only for explicit replay. Remove Skip. Collect actual operation type and terminology preferences and submit them through the existing update endpoint. Add a minimal authenticated Help page and Sidebar entry whose replay action opens onboarding without resetting completion.\n- **Where:** `frontend/src/router.tsx`; a new `frontend/src/auth/RequireOnboarding.tsx`; `frontend/src/pages/OnboardingPage.tsx`; a new `frontend/src/pages/HelpPage.tsx`; `frontend/src/components/layout/Sidebar.tsx`; onboarding API client/types; associated tests; both locale JSON files.\n- **Why:** Current onboarding is public, optional, and saves placeholders despite the approved required-once product decision and an existing persistence API.\n- **Preserve:** Existing login/session behavior, public forgot/reset paths, auth redirects, HttpOnly session cookie, CSRF attachment, onboarding endpoint request/response shape, and completed-user data.\n- **Verify:** Router/component tests cover unauthenticated access, incomplete-user gate, successful completion with selected values, completed-user bypass, replay from Help, intended-destination return, API loading/failure, and no Skip control. Run frontend HTTP/CSRF tests and relevant backend onboarding/auth tests unchanged.\n\n## Step 3 — Repair Login and Dashboard navigation truth\n\n- **What:** Remove mock-auth and unsupported individual-queue copy. Present only repository-supported capabilities. Replace invalid global Dashboard links with analysis-aware actions: when a valid recent/selected analysis exists, use its scoped workflow route; otherwise route to analysis selection/creation with clear copy.\n- **Where:** `frontend/src/pages/LoginPage.tsx`, `frontend/src/pages/DashboardPage.tsx`, `frontend/src/pages/DashboardPage.test.tsx`, any existing analysis selection helper, and both locale files.\n- **Why:** The current links fall through the router and the login page contradicts actual authentication and queue support.\n- **Preserve:** Login/Google-auth function calls, return-to navigation, dashboard queries, analysis ownership, and router path contracts.\n- **Verify:** Tests assert every rendered action resolves to an existing route for both “analysis available” and empty states; Login tests assert the false claims are absent; auth tests remain green.\n\n## Step 4 — Merge Guided Setup into canonical Setup\n\n- **What:** Move useful plain-language descriptions into `AnalysisSetupPage`, preserve `unknown` until explicitly answered, remove priority/automatic routing as a supported option, keep separate FIFO visibly unavailable in this version, and align patience-rate client validation with the existing positive backend rule. Replace the legacy Guided Setup screen with a redirect to the analysis-scoped canonical Setup and remove orphaned styles/imports only after usage search.\n- **Where:** `frontend/src/pages/AnalysisSetupPage.tsx`, `frontend/src/pages/GuidedSetupPage.tsx` or its removal, `frontend/src/router.tsx`, setup styles/types/tests, and both locales.\n- **Why:** The duplicate flow currently maps priority to shared FIFO and turns unanswered questions into definite assumptions.\n- **Preserve:** Existing setup API shape, existing stored setup values, the supported shared/single queue mappings, backend unsupported classification for separate queues, and the legacy URL as a working deep link.\n- **Verify:** Tests cover redirect behavior, native labelled controls, unknown serialization, shared/single serialization, separate limitation text, absence of priority, and rejection of zero patience rate before submission. Run analysis schema/API tests without changed fixtures.\n\n## Step 5 — Make Current analysis entirely evidence-derived\n\n- **What:** Normalize status matching, derive KPI hints and queue-pressure narrative from actual rows, show neutral/empty/unavailable outcomes, render backend model-selection explanations, and make arrival/model visuals complete or explicitly range-labelled with a complete data alternative.\n- **Where:** `frontend/src/pages/AnalysisCurrentPage.tsx`, relevant shared chart/table components, focused page tests, and both locales.\n- **Why:** Current hard-coded conclusions, case-sensitive counting, silent 13-period slice, and limited category treatment can misstate the analytical response.\n- **Preserve:** Raw API rows, selected model values, KPI calculations, model explanations, wait-time minute conversion, and model selection service.\n- **Verify:** Fixture tests cover zero/some/all critical periods, mixed-case status, missing values, more than 13 periods, and more than three model categories. Assert displayed explanation text comes from the API fixture. Run `tests/test_model_selection.py`.\n\n## Step 6 — Correct Optimize semantics without changing optimization\n\n- **What:** Rename the three goals as constraint profiles; display each profile’s utilization and maximum-wait settings; call results candidate/model-recommended until validation/Decision; explain that available cashier pool is a post-hoc coverage check, not an optimizer input; and treat zero as a provided pool.\n- **Where:** `frontend/src/pages/OptimizePage.tsx`, `frontend/src/pages/OptimizePage.test.tsx`, and both locales.\n- **Why:** Current copy claims a minimum-wait objective that is not implemented, overstates candidate status, and suppresses a zero-pool shortage.\n- **Preserve:** Optimization request payload, target values (0.85/no wait cap, 0.65/2 minutes, 0.75/5 minutes), optimizer objective/constraints/tie-breaking, saved scenario workflow, and response rendering.\n- **Verify:** Tests assert profile labels/settings, unchanged submitted payloads, candidate terminology, pool values of blank/zero/equal/short/excess, and no “minimum-wait objective” claim. Run `tests/test_optimization.py` and optimization API tests.\n\n## Step 7 — Remove synthetic Comparison evidence and unsafe cost claims\n\n- **What:** Remove the frontend radar scores/chart and ROI/calendar projections. Retain direct current-versus-candidate values and modeled cost difference labelled for the analyzed period. Introduce small pure formatting/summary helpers that ignore absent/non-finite values, preserve real zero, and return unavailable when no valid value or percentage denominator exists. Delete `radar.ts`, `roi.ts`, and their tests only after confirming no remaining imports.\n- **Where:** `frontend/src/pages/ComparisonPage.tsx`, `frontend/src/lib/radar.ts`, `frontend/src/lib/radar.test.ts`, `frontend/src/lib/roi.ts`, its tests, `frontend/src/components/charts/Charts.tsx` exports, focused Comparison tests, and both locales.\n- **Why:** The current scores have no backend provenance; ROI lacks investment cost; daily/monthly/annual scaling lacks an explicit base-period contract; missing metrics are treated as zero.\n- **Preserve:** Comparison API data, scenario identity, analytical/optimized values, direct staffing/wait/utilization/cost units, scenario-selection behavior, and save/evidence relationships.\n- **Verify:** Search shows no `computeRadarScores`, `RadarChart`, `computeRoi`, ROI, monthly, or annual claim in the rendered Comparison flow. Tests cover zero current cost, null/NaN/Infinity, mixed valid/missing rows, and direct source-value rendering. Run scenario-integrity and optimization tests.\n\n## Step 8 — Bind Simulation verdicts to persisted evidence\n\n- **What:** Read the failure cap from the persisted validation record’s `params.mc_failure_rate_cap` when rendering pass/fail and per-row comparison. Treat edits to current form inputs as next-run configuration; compare them with persisted parameters and display a rerun notice when changed. Complete tab/tabpanel relationships, keyboard behavior, and move clock updates out of continuously announced live regions.\n- **Where:** `frontend/src/pages/SimulationPage.tsx`, focused simulation tests, optional pure evidence-parameter helper, Live playback accessibility markup only, and both locales.\n- **Why:** Current form edits can change the displayed verdict without producing new evidence, while Decision correctly uses the persisted cap.\n- **Preserve:** DES/Monte Carlo request payloads, execution, seeds, event trace, playback reducer/timing, aggregate results, saved evidence, Decision derivation, and stale-state rules.\n- **Verify:** A test fixture with persisted cap 0.05 and edited form cap 0.20 continues to display the 0.05 evidence verdict plus rerun notice; rerunning replaces evidence normally. Tab keyboard/ARIA tests pass. Run `tests/test_simulation.py` and `tests/test_workflow_api.py`, especially aggregate/trace equivalence and Decision staleness.\n\n## Step 9 — Make report preview match each generated format\n\n- **What:** Replace the generic ten-section list with format-specific preview data. PDF preview lists title, Executive Summary, Segment Comparison, and Recommendations. Excel preview lists Summary, Segments, and conditional Recommendations exactly as the generator will create them. Remove absent simulation/ROI/methodology/appendix promises.\n- **Where:** `frontend/src/pages/ReportsPage.tsx`, report-page tests, and both locale files.\n- **Why:** Current preview promises content that the generated artifacts do not contain.\n- **Preserve:** `backend/api/reports.py`, `backend/reports/report_export.py`, Decision gating/source, export request/response behavior, filenames, PDF/Excel calculations, and wait-time minutes.\n- **Verify:** Frontend tests cover PDF, Excel with Recommendations, Excel without Recommendations, loading/error/disabled states, and exact section labels. Backend `tests/test_reports_api.py` passes; a generated PDF text extraction and Excel sheet-name check match the preview.\n\n## Step 10 — Phase 1 trust-repair gate\n\n- **What:** Stop feature work and validate P0-01 through P0-08 as one coherent workflow. Update tests that previously asserted defective behavior; do not weaken assertions merely to make them pass.\n- **Where:** Focused frontend tests from Steps 2-9 and protected backend suites; no new product behavior.\n- **Why:** Governance requires trust repair before broad visual refinement.\n- **Preserve:** All protected invariants in the specification.\n- **Verify:** Run the complete frontend unit suite and the focused backend protected suites. Manually exercise Login → onboarding → Setup → Current → Optimize → Compare → Simulation → Decision → Reports in English and Filipino. Record evidence that all eight P0 acceptance criteria pass. If any P0 remains, fix it within its approved step before proceeding.\n\n## Step 11 — Implement the approved semantic tokens, readable type, theme state, and locale state\n\n- **What:** Implement the primitive/semantic/component token contract in the approved Master for colors, typography, spacing, radii, elevation, focus, status, Current, and Optimized identities. Raise the body default to 16 px and functional table/form text to at least 14 px. Replace white-on-gold actions with the approved navy-on-gold pair. Make ThemeToggle React-state-driven and persistent. Synchronize `document.documentElement.lang` whenever i18next changes language.\n- **Where:** `design-system/novaq/MASTER.md` as authority; `frontend/src/styles/theme.css`, `frontend/src/styles/global.css`, `frontend/src/components/layout/ThemeToggle.tsx`, `frontend/src/lib/i18n.ts`, focused tests, and only the affected token consumers.\n- **Why:** Current styles contain undersized text, failing contrast combinations, bypassed inline colors, stale theme labelling, and a fixed document language.\n- **Preserve:** NovaQ navy/gold identity, existing light/dark preference keys, locale selection persistence, data/status meaning, and component behavior.\n- **Verify:** Unit tests cover repeated theme toggles, persistence, accessible label/state, initial locale, and live locale changes. Check normal text at 4.5:1 and large/non-text UI at 3:1 in both themes. Search affected workflow pages for superseded hard-coded colors and sub-14 px functional text.\n\n## Step 12 — Refine the responsive shell, navigation, icons, and action hierarchy\n\n- **What:** Add a visible-on-focus skip link and main target. Implement the Master’s persistent desktop Sidebar and in-flow compact navigation disclosure below 1024 px with no page-level horizontal overflow. Keep global destinations in Sidebar and make `WorkflowNavigator` the sole owner of the repository-executable Setup → Current → Optimize → Compare → Simulate → Decision → Reports sequence and Previous/Next actions. Replace structural glyph icons with local accessible `currentColor` SVG icons. Remove the duplicate Decision navigation after tracing it.\n- **Where:** `frontend/src/components/layout/AppLayout.tsx`, `Sidebar.tsx`, shared icon components/assets, `WorkflowNavigator.tsx` styling only, `frontend/src/components/decision/DecisionEndpoint.tsx` navigation only, `global.css`, and tests.\n- **Why:** Current narrow-screen shell behavior is incomplete, glyph icons are inconsistent, and duplicate actions weaken the workflow hierarchy.\n- **Preserve:** Route order, route paths, active-route matching, selected analysis/scenario context, Decision content, and logout/theme/language behavior.\n- **Verify:** Component tests cover skip-link target, accessible navigation names, active state, drawer/open-close keyboard behavior if used, and one canonical navigation area. Browser checks at 375/768/1024/1440 px show reachable navigation, visible primary actions, and no page-level horizontal scroll.\n\n## Step 13 — Apply shared accessibility to states, forms, tables, charts, and motion\n\n- **What:** Add consistent status/alert semantics to loading/error/empty/stale/success states; global `:focus-visible`; native labelled form controls and group legends; table captions/accessible names and header scopes; labelled contained table overflow; chart titles, unit-bearing summaries, non-color distinctions, and data alternatives; correct tab/tabpanel relationships; and a global reduced-motion override. Use at least 44×44 px interactive targets in compact layouts.\n- **Where:** `frontend/src/components/ui/ApiState.tsx`, shared chart files under `frontend/src/components/charts/`, reusable form/table styles, `SimulationPage.tsx`, major workflow page tables/charts, and `global.css`.\n- **Why:** These primitives currently produce inconsistent keyboard focus, announcements, chart/table access, and touch behavior across the app.\n- **Preserve:** Chart input arrays and units, form payload values, table ordering/content, async query/mutation behavior, and playback state/event semantics.\n- **Verify:** Testing Library uses role/name queries and keyboard interaction. Tests assert loading status, error alert, form labels/groups, table names/headers, chart summaries/data alternatives, tab relationships, and no continuous playback-clock live announcement. Manually test keyboard-only and reduced-motion modes.\n\n## Step 14 — Complete page-wide visual and bilingual consistency\n\n- **What:** Apply the approved tokens, hierarchy, spacing, state patterns, action variants, and localized copy to Login/auth, Dashboard, analyses list/new, analysis workspace, Setup, Current, Optimize, Compare, Simulation, Decision presentation, Reports, Datasets, Advanced/System, Account, Admin, and Help. Migrate remaining affected inline colors and hard-coded user-visible English. Do not add new analytical claims.\n- **Where:** The listed frontend pages/components, `global.css`, and both translation files. Backend code is out of scope.\n- **Why:** Cross-page consistency is part of the full refinement and must follow—not obscure—the trust repairs.\n- **Preserve:** Every page’s queries, mutations, route parameters, permissions, analytical values, evidence state, and workflow gating.\n- **Verify:** English and Filipino smoke tests cover every major route and absence of raw translation keys. Recursive key symmetry passes. Snapshot/semantic tests confirm loading, empty, error, stale, disabled, and populated states retain their behavior.\n\n## Step 15 — Perform integrated responsive and accessibility verification\n\n- **What:** Run the complete user workflow in representative data states and record manual evidence for both themes, both locales, keyboard, reduced motion, and all four target widths. Verify preview against generated PDF and Excel artifacts.\n- **Where:** Running frontend/backend stack or the established browser test harness; no speculative fixes outside approved acceptance criteria.\n- **Why:** Unit tests do not prove layout, contrast, focus visibility, overflow, or export-preview fidelity.\n- **Preserve:** Test data isolation, user ownership, generated report semantics, and saved evidence.\n- **Verify:** At 375, 768, 1024, and 1440 px confirm no clipped primary action or page overflow; focus order and skip link work; table regions are labelled; dialogs/tabs are keyboard-operable; light/dark contrast passes; motion reduction works; every P0 scenario is reproduced as fixed; PDF text and Excel sheet names match preview.\n\n## Step 16 — Run full regression gates and publish the verification record\n\n- **What:** Run every repository gate, inspect the final diff for protected-file changes, document all changed and untouched behavior, and update `handoff.md` only after success. Create a dated verification report under `docs/superpowers/reports/`.\n- **Where:** Entire repository; final report and handoff documentation.\n- **Why:** Completion requires proof that defects are gone and protected NovaQ behavior is unchanged.\n- **Preserve:** Unrelated pre-existing worktree changes; do not clean, reset, or overwrite them.\n- **Verify:** Run:\n  - `python -m pytest tests/ -x --tb=short`\n  - `ruff check .`\n  - `mypy .`\n  - `npm test`\n  - `npm run typecheck`\n  - `npm run lint`\n  - `npm run build`\n\n  Then compare `git status --short` and `git diff --stat` with the recorded baseline. The report must list focused/full test counts, manual viewport/theme/locale checks, P0 evidence, protected invariants and their passing tests, any warnings, and the exact files changed. Do not claim completion while a required gate or acceptance criterion is unresolved.\n\n## Stop conditions\n\nStop implementation and return to specification if any step would require:\n\n- separate-queue or priority-class analytical behavior;\n- a backend/API/report-generator contract change;\n- a different optimization objective or cost horizon;\n- DES, workflow evidence, or Decision semantic changes;\n- authentication/CSRF/authorization changes;\n- invented projection or investment metadata; or\n- a design change that hides or reinterprets source data.\n